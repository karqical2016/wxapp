{
  "name": "wxapp",
  "version": "1.0.0",
  "main": "main.js",
  "description": "wechat web client desktop",
  "scripts": {
    "dev": "electron .",
    "build": "electron-packager . wxapp --platform linux --arch x64 --icon=logo.ico --out dist/ --overwrite",
    "deb64": "electron-installer-debian --src dist/wxapp-linux-x64/ --dest dist/installers/ --arch amd64 --icon logo.ico"
  },
  "devDependencies": {
    "electron": "^9.2.0",
    "electron-installer-debian": "^3.1.0",
    "electron-packager": "^15.0.0"
  },
  "repository": {
    
  }
}
